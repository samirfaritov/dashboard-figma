{"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "dev": "parcel dashboard.html",
  "build": "parcel build dashboard.html"
},
  "dependencies": {
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
